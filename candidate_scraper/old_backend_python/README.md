# Old Backend (Pre-TypeScript)

This folder contains the original JavaScript backend files that were replaced during the TypeScript migration.

These files are kept for reference only. The active backend is now in `server/src/` using TypeScript.

## Original Files

- `index.js` — Express server entry point
- `services/apifyService.js` — Apify integration
- `services/sheetsService.js` — Google Sheets integration
- `utils/normalizeCandidate.js` — Candidate data normalization

## Migration Date

May 2026
