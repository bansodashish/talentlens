server {
    listen 80;
    server_name leedscrownbridge.co.uk www.leedscrownbridge.co.uk;

    # Redirect all HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$host$request_uri;

    # Max upload size (for CV uploads)
    client_max_body_size 20M;

    location / {
        proxy_pass         http://127.0.0.1:5001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;   # Allow long Apify searches
        proxy_send_timeout 300s;
    }
}

# HTTPS block — filled in automatically by certbot after: certbot --nginx
# server {
#     listen 443 ssl;
#     server_name leedscrownbridge.co.uk www.leedscrownbridge.co.uk;
#     ...
# }
